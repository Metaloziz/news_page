export enum ButtonsPagination {
  Zero,
  One,
  Two,
  Three,
  Four,
}


export enum Paths {
  MAIN = 'main',
  CURRENT_NEWS = 'current_news'
}

export enum RequestSource {
  NEWS = '/news',
  COMMENTS = '/comments',
}